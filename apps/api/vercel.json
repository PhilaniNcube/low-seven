{
  "buildCommand": "cd ../.. && turbo run build --filter=api",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index"
    }
  ],
  "functions": {
    "index.js": {
      "maxDuration": 30
    }
  }
}
